# Dongle Firmware (placeholder)

Planned USB receiver that terminates the wireless protocol and exposes HID to the host:
- maintains session/auth state
- translates wireless key reports to USB HID
- handles multi-host and pairing UX

Keep this aligned with the shared types in `proto/` and host simulations.***
